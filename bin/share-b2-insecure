#!/bin/bash

set -x

BUCKET=d4l3k-7d
FILE="$(openssl rand -hex 12)"

pv -W "$1" | rclone rcat "$BUCKET:$BUCKET/$FILE"

URL="https://f001.backblazeb2.com/file/$BUCKET/$FILE"

DOWNLOAD="curl $URL -o $(basename $1)"
echo $DOWNLOAD | xclip
echo $DOWNLOAD
